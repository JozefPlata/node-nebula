{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf ./../bjs && tsc && vite build && mkdir ./../bjs && mv dist/assets/* ./../bjs/babylon.js && cd .. && go run ./cmd/server",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vite": "^5.4.1"
  },
  "dependencies": {
    "@babylonjs/core": "^7.21.4",
    "@babylonjs/gui": "^7.21.4",
    "@babylonjs/materials": "^7.23.0"
  }
}
